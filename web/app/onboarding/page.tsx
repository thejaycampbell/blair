import { OnboardingWizard } from '@/components/onboarding-wizard';

export const metadata = { title: 'Set up Blair — AI CMO' };

export default function OnboardingPage() {
  return <OnboardingWizard />;
}
